<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.javen.dao.TeacherDao" >

    <select id="SelectCount"  resultType="java.lang.Integer" >
        SELECT count(*) FROM t_teacher;
    </select>

    <select id="selectAll" resultType="com.javen.model.Teacher" parameterType="java.lang.Integer">
        select * from t_teacher
        limit #{param1},#{param2}
    </select>

    <insert id="insert" parameterType="com.javen.model.Teacher">
        insert into t_teacher (name,phone,passWord,className) values (#{name},#{phone},#{passWord},#{className})
    </insert>

    <delete id="delete" parameterType="java.lang.Integer">
        delete from t_teacher where id = #{id}
    </delete>

    <update id="update" parameterType="com.javen.model.Teacher">
        update t_teacher set name = #{name},phone = #{phone},passWord = #{passWord},className = #{className} where id = #{id}
    </update>

    <select id="likeByName" resultType="com.javen.model.Teacher" parameterType="java.lang.String">
        select * from t_teacher where phone like '%${value}%'
    </select>

    <select id="GetTeacher" resultType="com.javen.model.Teacher" parameterType="java.lang.String">
        select
            *
        from t_teacher
        where className = #{className}
    </select>

    <select id="SelectNullClass" resultType="java.lang.Integer" parameterType="java.lang.String">
        select count(*) from t_teacher where className = #{className}
    </select>

    <update id="updateClass" parameterType="com.javen.model.Teacher">
        update t_teacher set className = #{className} where name = #{name}
    </update>
</mapper>